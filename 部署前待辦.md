# shulincc-album 上線步驟

程式碼、內容 schema、CMS 設定都已寫完,115 春季 82 門課真實內容也已轉入,並在本機驗證過 `npm install && npm run build` 成功(84 個頁面正常產出,含相簿式/文章式兩種版面)。以下是接下來要做的事,照順序做,沙盒環境沒有權限代做,都需要你在自己的 GitHub / Cloudflare 帳號操作:

## 第 1 步:建立 GitHub repo

1. 到 GitHub 開一個新 repo,命名 `shulincc/shulincc-album`(跟主站 `shulincc-site` 是完全獨立的兩個 repo,不是同一個,理由是這兩個專案沒有共用程式碼,拆開後權限也比較好管理——以後想找人只協助管理相簿內容,可以只開放這個 repo)。
2. 把 `starter/shulincc-album/` 這個資料夾**裡面的內容**push 上去,當作這個新 repo 的根目錄(不是連同 `starter/shulincc-album` 這層資料夾名稱一起搬過去)。沙盒沒有 git push 權限,這步要你在本機或用 GitHub Desktop/網頁操作。
3. 如果實際建立的 repo 名稱跟 `shulincc/shulincc-album` 不一樣,記得回來把 `public/admin/config.yml` 裡 `backend.repo` 那一行改成實際名稱。

## 第 2 步:設定 R2 儲存

1. 確認 R2 bucket——已經決定沿用主站規劃的同一顆 `shulincc-assets`,不用另外開新的。共用同一顆桶子沒問題,因為主站舊檔案走 `uploads/` 前綴、相簿站走 `album/{year}/` 前綴,兩邊不會互相干擾。
2. 建立 R2 API Token,取得 `access_key_id` 與 `account_id`,填入 `public/admin/config.yml` 的 `media_libraries.cloudflare_r2`(目前是佔位值,不能直接上線用)。
3. 確認 R2 自訂網域 `assets.shulincc.org` 已綁定(沿用主站的話應該已經存在)。
4. 到 R2 bucket 設定 CORS,允許 `album.shulincc.org`(以及本機測試用的 `localhost`)可以上傳。CORS 是綁在整顆 bucket 上,不會因前綴不同自動分開授權——以後如果主站自己的 CMS 也改走 R2 直傳,要記得把主站後台網域也加進允許清單。
5. 把相簿站這 384 張照片上傳到乾淨的 `album/115/` 路徑(檔名跟 CMS 裡已經寫好的一致就能自動接上)。這些照片目前實體檔案還在舊 Weebly 路徑(`uploads/7/6/2/5/7625094/`),上傳時不要照抄那個舊路徑,直接放進 `album/115/`——想要的話我可以幫你寫一個批次上傳腳本。

## 第 3 步:建立 Cloudflare Pages 專案

1. 新增一個獨立的 Pages 專案給 `shulincc-album`,連到第 1 步建立的新 repo。
2. build command:`npm run build`,output directory:`dist`。
3. root directory 留空/預設值即可(因為是獨立 repo,repo 根目錄就是專案根目錄,不用像 monorepo 那樣指定子資料夾)。

## 第 4 步:綁定網域

到 Cloudflare DNS 加一筆 CNAME,把 `album.shulincc.org` 指到第 3 步建立的 Pages 專案。

## 第 5 步:測試

1. 用瀏覽器開 `album.shulincc.org/admin`,用 GitHub 帳號登入——OAuth Worker(`sveltia-cms-auth.mail-25e.workers.dev`)是通用的,不用重新申請,但要確認你的 GitHub 帳號對新 repo 有寫入權限,不然會卡在授權那一步。
2. 試著編輯一篇文章、上傳一張測試圖片,確認 R2 直傳跟自動轉 WebP 都正常運作(這件事沙盒沒辦法測,因為沒有真實 R2 憑證,一定要你自己實際測一次)。

---

## 之後會用到的例行維護(不是上線步驟,先記著)

**新學年度/新學期別:** 不用改程式碼。去後台「年度清單」新增一筆(例如 116),「學期別清單」新增一筆(例如新的分類)即可,同仁自己就能做。另外記得手動把 `config.yml` 裡的 `prefix: 'album/115/'` 改成新學年度(例如 `album/116/`),這行還是要靠你改。

**清舊年度空間時:**
- 只勾要刪的那個資料夾(例如 `album/114/`)刪除,**絕對不要點「Empty bucket」**——這顆 bucket 主站舊檔案跟相簿站所有年度都放在一起,Empty bucket 會不分青紅皂白整顆桶子清空。
- R2 刪除不會連動刪除 CMS 裡的文章,兩者要分開做:光刪 R2 檔案,`classalbum` 裡引用那個年度的文章還在,圖片會變 404 破圖;想讓那個年度整個從網站消失,要同時去 CMS(或 GitHub)把對應文章也刪掉或搬走(目前沒有一鍵連動的功能,細節見規劃討論)。
- 沒有回收桶,刪除無法復原,動手前先確認電腦上還留著一份備份。
- 剛刪完馬上整理網頁還看得到舊圖,通常只是 Cloudflare 邊緣快取還沒過期,不代表沒刪成功。
