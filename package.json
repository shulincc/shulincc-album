{
  "name": "shulincc-album",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-redirects.mjs && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^6.0.0",
    "pagefind": "^1.5.2"
  }
}
